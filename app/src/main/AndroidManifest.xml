<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cb.SmartGroups">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CoordinApp">

        <meta-data android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name="com.cb.SmartGroups.activities.LoginActivity"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Coordinapp.NoActionBar"
            android:screenOrientation="portrait"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.cb.SmartGroups.activities.MainActivity_Student"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Coordinapp.NoActionBar"
            android:screenOrientation="portrait"
            />
        <activity
            android:name="com.cb.SmartGroups.activities.MainActivity_Teacher"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Coordinapp.NoActionBar"
            android:screenOrientation="portrait"
            />
        <activity android:name="com.cb.SmartGroups.activities.ChatActivity"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Coordinapp.NoActionBar"
            android:windowSoftInputMode=""
            android:screenOrientation="portrait"
            />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>